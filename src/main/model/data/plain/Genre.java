package model.data.plain;

// CLASS DOCUMENTATION: a finite type of Genres that a song
// can be. Can be added to/changed/reduced without affecting
// other parts of the project.

public enum Genre { // 'enum' learned/referenced from LibrarySystem project on EdX
    UNKNOWN,
    RNBSOUL,
    JAZZ,
    BLUES,
    CHILDRENS,
    CLASSICAL,
    COUNTRY,
    ELECTRONIC,
    FOLKNWORLD,
    HIPRAP
}
